import { checkForUrl } from '../src/client/js/urlChecker'

describe("Testing the submit funtctionality", () => {
    test("Testing the checkForUrl() function", () => {

        expect(checkForUrl).toBeDefined();
    })
})